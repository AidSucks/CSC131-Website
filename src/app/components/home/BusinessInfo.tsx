// When the database is set up, we can use the below function.
// export async function fetchBusinessInfo() {
//   const response = await fetch(process.env.NEXT_PUBLIC_API_URL + "/api/business-info");
//   const data =  await response.json();

//   return data;
// }